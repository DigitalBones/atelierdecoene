<section class="py-5" id="defaultCta4">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-lg-8">
                <h2>{{ section.title }}</h2>
                <p>{{ section.subtitle }}</p>
                {% if section.button %}
                    {% include '_includes/_button.twig' with { 'button': section.button } %}
                {% endif %}
            </div>
        </div>
    </div>
</section>